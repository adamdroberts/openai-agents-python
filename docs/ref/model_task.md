# `Model tasks`

Model tasks are single-call helpers for cases where application code wants model output without
running the full agent loop. For usage guidance and examples, see the Direct model tasks section in
the Running agents guide.

::: agents.model_task
